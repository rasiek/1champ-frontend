import { TournamentSchemaDirective } from './tournament-schema.directive';

describe('TournamentSchemaDirective', () => {
  it('should create an instance', () => {
    const directive = new TournamentSchemaDirective();
    expect(directive).toBeTruthy();
  });
});
